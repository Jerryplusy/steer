# ===========================================================================
# Steer 项目依赖
#
# 安装：pip install -r requirements.txt
# 注意：必须在 conda steer 环境下装，否则会装到默认 Python
#   /opt/miniconda3/envs/steer/bin/python -m pip install -r requirements.txt
# ===========================================================================

# ---- 深度学习基座 ----
torch>=2.5.0
transformers>=4.45.0,<6.0
accelerate>=1.0.0
safetensors>=0.4.0
huggingface_hub>=1.0.0

# ---- 配置系统（EasyEdit2 强依赖）----
hydra-core>=1.3.0
omegaconf>=2.3.0
python-dotenv>=1.0.0
PyYAML>=6.0

# ---- 数据 / 评测 ----
pandas>=2.0.0
numpy>=1.26,<3.0
scipy>=1.10
pyarrow>=15.0
nltk>=3.8
tqdm>=4.66
datasets>=2.14

openai>=1.30
requests>=2.31

# ---- sklearn / 视觉 / 工具 ----
scikit-learn>=1.3
matplotlib>=3.7
Pillow>=10.0
einops>=0.7
jinja2>=3.1

